import { Route, Routes } from 'react-router-dom';

function UserRoutes(): JSX.Element {
  return <Routes>{/* <Route path="login" element={<ViewUserLogin />} /> */}</Routes>;
}

export default UserRoutes;
